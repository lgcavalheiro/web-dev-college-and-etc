{
  "name": "trabalho_academico_av1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "browserslist": [
    "last 1 Chrome versions"
  ],
  "scripts": {
    "purge": "rm -rf ./public/*",
    "dev": "parcel watch ./frontend/html/*.html --out-dir public --no-source-maps",
    "build": "parcel build ./frontend/html/*.html --out-dir public --no-source-maps",
    "serve": "node backend/server.js",
    "go": "yarn purge && yarn build && yarn serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "json-server": "^0.16.1",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.11"
  }
}
